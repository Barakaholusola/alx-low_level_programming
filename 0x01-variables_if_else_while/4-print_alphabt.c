#include <stdio.h>
/**
 * main- Entry point
 *
 * Description: Prints alphabet in lowercase, followed by a new line
 *
 * Return: (0);
 */
int main(void)
{
	int alphabt;

	alphabt = 'a';
	while (alphabt <= 'z')
	{
		if (alphabt == 'e')
		{
		}
		else
		if (alphabt == 'q')
		{
		}
		else
		putchar(alphabt);
		alphabt++;
	}
	putchar('\n')
		return (0);
}
